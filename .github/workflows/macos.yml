name: MacOS build

on: [push]

jobs:
  macos-build:
    name: MacOS build
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
      - run: brew install libsdl2-dev libfreetype6-dev zlib-devel libpng-devel
      - run: make e2e_test
